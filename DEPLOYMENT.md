# 🚀 Деплой на GitHub Pages

## 📋 Подготовка

### 1. Создайте репозиторий на GitHub
1. Перейдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий `MusicTrainer`
4. Оставьте публичным (Public)
5. НЕ инициализируйте с README, .gitignore или лицензией
6. Нажмите "Create repository"

### 2. Инициализируйте Git локально (если ещё не сделано)

```bash
# Инициализация Git
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Music Trainer app with tests"

# Переименуйте ветку в main (если нужно)
git branch -M main

# Подключите удалённый репозиторий (замените YOUR-USERNAME на ваш username)
git remote add origin https://github.com/YOUR-USERNAME/MusicTrainer.git

# Отправьте код на GitHub
git push -u origin main
```

## 🔧 Настройка GitHub Pages

### Автоматический деплой через GitHub Actions (Рекомендуется)

1. **Перейдите в Settings репозитория**
   - Откройте ваш репозиторий на GitHub
   - Перейдите в **Settings** → **Pages**

2. **Настройте источник**
   - В разделе "Build and deployment"
   - Source: выберите **GitHub Actions**

3. **Первый деплой**
   - GitHub Actions автоматически запустится после push в main
   - Через 2-3 минуты сайт будет доступен по адресу:
     ```
     https://YOUR-USERNAME.github.io/MusicTrainer/
     ```

4. **Проверка статуса**
   - Перейдите во вкладку **Actions**
   - Убедитесь, что workflow "Deploy to GitHub Pages" завершился успешно ✅

### Ручной деплой (Альтернативный способ)

Если вы хотите деплоить вручную:

```bash
# Установите gh-pages пакет
npm install --save-dev gh-pages

# Соберите и задеплойте
npm run deploy
```

После выполнения:
1. Перейдите в **Settings** → **Pages**
2. Source: выберите **Deploy from a branch**
3. Branch: выберите **gh-pages** и папку **/ (root)**
4. Нажмите **Save**

## 🔄 Обновление сайта

### Автоматически (GitHub Actions)
```bash
# Внесите изменения в код
git add .
git commit -m "Описание изменений"
git push

# GitHub Actions автоматически пересоберёт и задеплоит сайт
```

### Вручную
```bash
npm run deploy
```

## ⚙️ Настройка base path

Если название вашего репозитория **НЕ** `MusicTrainer`:

1. Откройте `vite.config.ts`
2. Измените строку:
   ```typescript
   base: process.env.GITHUB_ACTIONS ? '/YOUR-REPO-NAME/' : '/',
   ```
3. Замените `YOUR-REPO-NAME` на название вашего репозитория

## 🌐 Собственный домен (Опционально)

Если у вас есть собственный домен:

1. Создайте файл `public/CNAME` с содержимым:
   ```
   yourdomain.com
   ```

2. В настройках DNS вашего домена добавьте записи:
   ```
   A    185.199.108.153
   A    185.199.109.153
   A    185.199.110.153
   A    185.199.111.153
   ```

3. В GitHub Settings → Pages укажите ваш домен в поле "Custom domain"

## 📁 Структура деплоя

```
.github/
  workflows/
    deploy.yml          # GitHub Actions workflow
public/
  .nojekyll            # Файл для GitHub Pages
vite.config.ts         # Конфигурация с base path
package.json           # Скрипт для ручного деплоя
```

## 🐛 Устранение проблем

### Проблема: Сайт не загружается или показывает 404

**Решение:**
1. Убедитесь, что `base` в `vite.config.ts` соответствует названию репозитория
2. Проверьте, что GitHub Pages включен в Settings
3. Подождите 2-3 минуты после деплоя

### Проблема: CSS/JS файлы не загружаются

**Решение:**
1. Убедитесь, что файл `public/.nojekyll` существует
2. Проверьте консоль браузера на ошибки путей
3. Убедитесь, что `base` в `vite.config.ts` заканчивается на `/`

### Проблема: GitHub Actions падает с ошибкой

**Решение:**
1. Проверьте логи в разделе Actions
2. Убедитесь, что все зависимости установлены (`npm ci`)
3. Проверьте права доступа в Settings → Actions → General

### Проблема: Старая версия сайта после обновления

**Решение:**
1. Очистите кэш браузера (Ctrl+F5 или Cmd+Shift+R)
2. Откройте в режиме инкогнито
3. Подождите несколько минут для обновления CDN

## 📊 Мониторинг деплоя

### Просмотр статуса деплоя
1. Перейдите в **Actions** на GitHub
2. Найдите последний workflow run
3. Проверьте статус каждого шага

### Просмотр логов
```bash
# Локально проверьте сборку
npm run build

# Запустите превью
npm run preview
```

## 🎯 Чек-лист перед деплоем

- [ ] Git репозиторий инициализирован
- [ ] Код залит на GitHub
- [ ] `vite.config.ts` настроен с правильным base path
- [ ] GitHub Pages включен в Settings
- [ ] Source установлен на GitHub Actions
- [ ] Файл `.nojekyll` существует в папке `public/`
- [ ] GitHub Actions workflow существует в `.github/workflows/deploy.yml`
- [ ] Первый push в main ветку выполнен

## 🔗 Полезные ссылки

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html#github-pages)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)

## 📝 Примечания

- GitHub Pages **бесплатен** для публичных репозиториев
- Деплой через Actions занимает 2-3 минуты
- Сайт обновляется автоматически при каждом push в main
- Лимиты GitHub Pages: 1GB хранилища, 100GB bandwidth/месяц
- Сайт доступен по HTTPS автоматически

---

## 🎉 Поздравляем!

Ваш музыкальный тренажёр теперь доступен онлайн!

**URL вашего сайта:**
```
https://YOUR-USERNAME.github.io/MusicTrainer/
```

Поделитесь ссылкой с друзьями! 🎵
