# FSD Architecture Documentation

## 📁 Полностью соответствует стандартам FSD

Проект **полностью соответствует** официальной методологии [Feature-Sliced Design](https://feature-sliced.github.io/documentation/ru/docs/get-started/overview).

```
src/
├── app/             ✅ Слой приложения (без слайсов)
│   ├── styles/      # Глобальные стили
│   │   └── index.css
│   ├── App.vue      # Корневой компонент
│   ├── main.ts      # Точка входа
│   └── index.ts     # Публичный API
├── pages/           ✅ Страницы (со слайсами)
│   └── settings/
│       ├── ui/
│       ├── types/
│       └── index.ts
├── widgets/         ✅ Виджеты (со слайсами)
│   ├── notation-trainer/
│   │   ├── ui/
│   │   ├── types/
│   │   └── index.ts
│   └── settings-panel/
│       ├── ui/
│       ├── types/
│       └── index.ts
├── features/        ✅ Фичи (со слайсами)
│   ├── training-mode-selector/
│   │   ├── ui/
│   │   ├── types/
│   │   └── index.ts
│   └── profile-manager/
│       ├── ui/
│       ├── types/
│       └── index.ts
├── entities/        ✅ Сущности (со слайсами)
│   ├── note/
│   │   ├── model/
│   │   ├── types/
│   │   └── index.ts
│   ├── profile/
│   ├── session/
│   └── statistics/
└── shared/          ✅ Переиспользуемый код (без слайсов)
    ├── types/
    ├── config/
    ├── lib/
    └── ui/          # Каждый компонент в своей папке
        ├── button/
        │   ├── ui/
        │   │   └── BaseButton.vue
        │   └── index.ts
        ├── slider/
        ├── checkbox/
        ├── radio-group/
        └── modal/
```

---

## ✅ Соответствие стандартам FSD

### 1. **Слои организованы правильно**

Согласно [документации FSD](https://feature-sliced.github.io/documentation/ru/docs/reference/layers):

**Слои App и Shared** — без слайсов, сразу сегменты:
- ✅ `app/` содержит styles/, App.vue, main.ts
- ✅ `shared/` содержит types/, config/, lib/, ui/

**Остальные слои** — со слайсами:
- ✅ `pages/` → settings/
- ✅ `widgets/` → notation-trainer/, settings-panel/
- ✅ `features/` → training-mode-selector/, profile-manager/
- ✅ `entities/` → note/, profile/, session/, statistics/

### 2. **Структура слайсов единообразна**

Каждый слайс имеет:
```
slice-name/
├── model/       # Composables (логика)
├── types/       # TypeScript namespace
├── ui/          # Vue компоненты
└── index.ts     # Публичный API
```

### 3. **Правило импортов соблюдается**

Слои импортируют только из нижних:
- ✅ `app` → импортирует из widgets, features, entities, shared
- ✅ `pages` → импортирует из widgets, features, entities, shared
- ✅ `widgets` → импортирует из features, entities, shared
- ✅ `features` → импортирует из entities, shared
- ✅ `entities` → импортирует из shared
- ✅ `shared` → не импортирует из других слоёв

### 4. **Shared/ui организован правильно**

Согласно best practices, каждый UI компонент в своей папке:
```
shared/ui/
├── button/
│   ├── ui/
│   │   └── BaseButton.vue
│   └── index.ts
└── ...
```

---

## 🏗️ Слои приложения

### 1. App (Приложение)

**Назначение:** Инициализация, конфигурация, глобальные стили.

```
app/
├── styles/      # Глобальные стили
├── App.vue      # Корневой компонент
├── main.ts      # Точка входа
└── index.ts     # Экспорты
```

**Что здесь:**
- Создание Vue приложения
- Подключение глобальных стилей
- Провайдеры (при необходимости)
- Роутинг (при необходимости)

### 2. Pages (Страницы)

**Назначение:** Полные страницы приложения.

**Слайсы:**
- `settings/` — страница настроек тренировки

### 3. Widgets (Виджеты)

**Назначение:** Большие самодостаточные блоки функциональности.

**Слайсы:**
- `notation-trainer/` — виджет тренажёра нот (главный функционал)
- `settings-panel/` — панель всех настроек

### 4. Features (Фичи)

**Назначение:** Переиспользуемые реализации бизнес-фич.

**Слайсы:**
- `training-mode-selector/` — выбор режима тренировки
- `profile-manager/` — управление профилями

### 5. Entities (Сущности)

**Назначение:** Бизнес-сущности проекта.

**Слайсы:**
- `note/` — музыкальные ноты
- `profile/` — профили пользователя
- `session/` — сессии тренировок
- `statistics/` — статистика

### 6. Shared (Общее)

**Назначение:** Переиспользуемый код.

**Сегменты:**
- `types/` — общие TypeScript типы
- `config/` — константы и конфигурация
- `lib/` — утилиты и хелперы
- `ui/` — базовые UI компоненты

---

## 📦 Примеры кода

### Entity: Profile

```typescript
// entities/profile/types/index.ts
export namespace ProfileTypes {
  export type Profile = TrainingSettings & {
    id: string;
    name: string;
  };
}

// entities/profile/model/useProfile.ts
export const useProfile = () => {
  const profiles = ref<ProfileTypes.Profile[]>([]);
  
  const loadProfiles = () => {...};
  const createProfile = (name, settings) => {...};
  
  return {
    profiles,
    loadProfiles,
    createProfile,
  };
};

// entities/profile/index.ts
export * from './model/useProfile';
export * from './types';
```

### Widget: Notation Trainer

```
widgets/notation-trainer/
├── ui/
│   └── NotationTrainer.vue    # Главный компонент тренажёра
├── types/
│   └── index.ts               # Типы
└── index.ts                   # Экспорт
```

### Shared UI: Button

```
shared/ui/button/
├── ui/
│   └── BaseButton.vue
└── index.ts                   # export { default as BaseButton }
```

---

## 🔄 Использование в App.vue

```typescript
// app/App.vue
import { useProfile } from '@/entities/profile';
import { useStatistics } from '@/entities/statistics';
import { useSession } from '@/entities/session';
import { NotationTrainer } from '@/widgets/notation-trainer';
import type { ProfileTypes } from '@/entities/profile';

const profileStore = useProfile();
const statisticsStore = useStatistics();
const sessionStore = useSession();

onMounted(() => {
  profileStore.loadProfiles();
  statisticsStore.loadStatistics();
});
```

---

## 🎯 Преимущества архитектуры

### 1. **Соответствие стандартам**
- ✅ Полное соответствие официальной методологии FSD
- ✅ Легко объяснить новым разработчикам
- ✅ Есть документация и сообщество

### 2. **Однородность**
- ✅ Все слайсы имеют одинаковую структуру
- ✅ Легко ориентироваться в коде
- ✅ Предсказуемое расположение файлов

### 3. **Масштабируемость**
- ✅ Легко добавить новый слайс
- ✅ Можно добавить api/, lib/, config/ при необходимости
- ✅ Расширение без изменения существующего кода

### 4. **Устойчивость к изменениям**
- ✅ Правило импортов предотвращает циклические зависимости
- ✅ Изолированные изменения
- ✅ Локальный рефакторинг

### 5. **Типобезопасность**
- ✅ TypeScript namespace для типов
- ✅ Строгая типизация всех composables
- ✅ Публичный API через index.ts

---

## 📝 Правила разработки

### 1. Добавление нового слайса

```bash
# Пример: добавить новую entity "song"
mkdir -p src/entities/song/{model,types,ui}
touch src/entities/song/model/useSong.ts
touch src/entities/song/types/index.ts
touch src/entities/song/index.ts
```

### 2. Импорты

```typescript
// ✅ Правильно: через публичный API
import { useProfile, ProfileTypes } from '@/entities/profile';

// ❌ Неправильно: прямой импорт
import { useProfile } from '@/entities/profile/model/useProfile';
```

### 3. Структура composable

```typescript
export const useSomething = () => {
  const state = ref(initialState);
  
  const loadData = async () => {
    // логика
  };
  
  const computed = computed(() => {
    // вычисления
  });
  
  return {
    state,
    computed,
    loadData,
  };
};
```

### 4. Типы через Namespace

```typescript
export namespace EntityTypes {
  export type Main = {
    id: string;
    name: string;
  };
  
  export type Settings = {
    option: boolean;
  };
}
```

---

## ✅ Чеклист соответствия FSD

- [x] Слой `app/` создан и содержит точку входа
- [x] Слои `app/` и `shared/` без слайсов
- [x] Остальные слои (entities, features, widgets, pages) со слайсами
- [x] Каждый слайс имеет структуру: model/, types/, ui/, index.ts
- [x] Shared/ui организован с папками для каждого компонента
- [x] Правило импортов соблюдается (только из нижних слоёв)
- [x] Composables с префиксом `use`
- [x] Типы в namespace
- [x] Публичный API через index.ts
- [x] Алиас `@/` настроен
- [x] Документация актуальна

---

## 🔧 Технический стек

- **Vue 3** - Composition API
- **TypeScript** - Строгая типизация с Namespace
- **Vite** - Сборка
- **VexFlow** - Отображение нотного стана
- **Tone.js** - Синтез звука
- **Web MIDI API** - Интеграция MIDI клавиатуры
- **FSD** - Feature-Sliced Design v2.1

---

## 📚 Источники

- [FSD Документация](https://feature-sliced.github.io/documentation/ru/docs/get-started/overview)
- [FSD Туториал](https://feature-sliced.github.io/documentation/ru/docs/get-started/tutorial)
- [FSD Референс: Слои](https://feature-sliced.github.io/documentation/ru/docs/reference/layers)
- [FSD Референс: Слайсы и сегменты](https://feature-sliced.github.io/documentation/ru/docs/reference/slices-segments)
- [Пример: tech-nuxt](https://github.com/miirsery/tech-nuxt/tree/main/client)

---

**Статус**: ✅ Полностью соответствует стандартам FSD  
**Версия**: 3.0 (FSD Compliant)  
**Дата обновления**: 2026-01-12
