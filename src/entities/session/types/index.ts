import type { TrainingSession } from '@/shared/types';

/**
 * Типы для сессии тренировки
 */
export namespace SessionTypes {
  /**
   * Сессия тренировки (экспортируем из shared для совместимости)
   */
  export type Session = TrainingSession;
}
